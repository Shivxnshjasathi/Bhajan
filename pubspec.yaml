name: bhajan
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: ^3.6.0
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_pdfview: ^1.3.4
  path_provider: ^2.1.5
  google_fonts: ^6.2.1
  fuzzy: ^0.5.1
  line_icons: ^2.0.3
  flutter_launcher_icons: ^0.14.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - bhajanPDF/
    - assets/
    - assets/logo-add-bg.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo-add-bg.png"
 